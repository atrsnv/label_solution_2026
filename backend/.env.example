# Server
PORT=4000
NODE_ENV=development

# JWT
JWT_SECRET=super_secret_change_me
JWT_EXPIRES_IN=7d

# Database (SQLite for hackathon; switch to PostgreSQL for prod / DataLens)
DATABASE_URL="file:./dev.db"

# Default label share for new artists (percent, 0..100)
DEFAULT_LABEL_SHARE=30
